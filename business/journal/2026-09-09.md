# 2026-09-09

## What happened

Kicked off the business coaching/journal system. Set up `business/` as a separate
module in the mikistec repo (not part of the Next.js app) to hold context, journal,
notes, and a living strategy doc — kept in the repo instead of Claude's own memory so
the user can read/edit it directly.

Reviewed all three projects to build context: mikistec (this repo, the landing
page/offer), qviks (`../qviks`, the SaaS product), stec.sk (`../project-resume`, the
CV/portfolio). Wrote up `context/overview.md`, `context/qviks-overview.md`,
`context/stec-overview.md`, and a first-pass `STRATEGY.md`.

Confirmed from the code: the offer/pricing section on the mikistec landing page is
currently commented out (`src/app/page.tsx`), so the live site shows no price. stec.sk
is not linked from the mikistec site anywhere.

## Numbers

Not known yet — no lead/pipeline tracking exists outside the Formspree inbox. First
thing to fill in on the next entry: how many leads have come in so far, how many
calls/clients to date.

## Blockers

None yet — this was a setup day, not an execution day.

## Next

1. Decide: restore the €990 flat-price offer, or a different price/packaging — then
   update the landing page.
2. Pull whatever lead history exists from Formspree and get it into a qviks pipeline
   so we have a real baseline number.
3. Pick this week's outreach target list.

## Update — stec.sk and own-CRM decisions

Reviewed the two open findings that had a clear call to make:

- **stec.sk (finding 5):** corrected — it's Miki's corporate job-search CV, not a
  freelance credibility asset. Decision: keep it fully separate from mikistec, no
  link either direction. Different, mildly conflicting audience signals
  (available-for-hire employee vs. running an ongoing agency). Updated
  `context/stec-overview.md` and `context/overview.md` to reflect this.
- **Personal-brand content on mikistec** (photography, design, personal history) —
  a real idea Miki's been considering, but agreed to defer: fundamentals (priced
  offer, outreach cadence, pipeline) aren't in place yet, and this doesn't fix any
  of those. Logged in `notes/backlog-ideas.md` with a reopen trigger (once the
  core loop is working and the question becomes raising conversion, not having a
  loop at all).
- **Own-CRM / pipeline (finding 3):** confirmed as a this-week priority — advised
  starting lightweight (one qviks tenant, manual lead logging), no
  Formspree→qviks automation yet since there's no volume data to justify it.

No comment yet from Miki on findings 1, 2, 4 (offer/price, outreach cadence, qviks
upsell trigger) — still open.
